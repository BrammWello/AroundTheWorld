import 'package:flutter/material.dart';
import 'package:around_the_world/screens/profile_completed/components/body.dart';

class ProfileCompleted extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
